[package]
name = "snarkvid-h264-decoder"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]

[features]
default = ["std"]
std = []

[dependencies]
# Hand-rolled error type so the decoder stays no_std-compatible for
# eventual zkVM use.
