[package]
name = "snarkvid-spike-risc0-methods"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

# methods/build.rs walks methods/guest and produces the ELF + image ID
# that the host crate consumes. See https://docs.risczero.com for the
# canonical pattern.

[build-dependencies]
# risc0-build = { workspace = true }

[package.metadata.risc0]
methods = ["guest"]
