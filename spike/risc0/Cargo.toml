# Nested workspace for the RISC Zero side. The guest crate inside
# methods/guest is intentionally NOT a member here — it has a different
# target (riscv32im) and is built by methods/build.rs.

[workspace]
resolver = "2"
members = [
    "host",
    "methods",
]
exclude = [
    "methods/guest",
]

[workspace.package]
edition = "2021"
license = "Apache-2.0"
publish = false

# Pin actual versions on day 1 — these are placeholders.
[workspace.dependencies]
# risc0-zkvm = "1"
# risc0-build = "1"
# bytemuck = "1"
# clap = { version = "4", features = ["derive"] }
# anyhow = "1"
# serde = { version = "1", features = ["derive"] }
# serde_json = "1"
# sha2 = "0.10"
